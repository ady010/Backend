<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex justify-center items-center h-screen gap-5">
    <% users.forEach(user => { %>
      <div class="bg-white h-[20rem] w-[15rem] shadow-lg rounded-lg gap-5 justify-center flex flex-col items-center">
        <img src="<%= user.img %>" alt="User Image" class="w-28 h-28 rounded-full object-cover mb-4">
        <h3 class="text-2xl font-semibold text-gray-800"><%= user.name %></h3>
      </div>
    <% }) %>
  </body>  
</html>