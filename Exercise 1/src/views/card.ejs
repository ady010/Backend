<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <h1 class="text-center text-4xl font-semibold" >Users</h1>
    <div class="cards grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 p-6">
        <% getData.forEach(user => { %>
          <a href="/users/<%= user._id %>" class="block border-2 rounded-lg shadow-md hover:shadow-lg transition-shadow duration-200">
            <h1 class="text-xl font-semibold text-gray-800 p-4 border-b border-gray-200"><%= user.name %></h1>
            <img src="<%= user.img %>" alt="<%= user.name %>" class="w-full h-64 object-cover rounded-b-lg">
          </a>
        <% }) %>
      </div>   
</body>
</html>